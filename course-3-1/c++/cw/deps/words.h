#ifndef WORDS_H
#define WORDS_H

#include <string>
#include <vector>

std::vector<std::string> generate_letters(int length);

std::vector<std::string> generate_words(int length);

std::vector<std::string> generate_syllables(int length);

#endif
